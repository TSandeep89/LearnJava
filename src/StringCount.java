public class StringCount {

    public static void main(String[] args) {

        String str = "Sandeep";
        int len = str.length();
        System.out.println(len);
        String rev = "";

       // if(str.contains("s") || ("a") || ("n") || ("d") || ("e") || ("e") ||("p"));

//        for(int i = len - 1; i >= 0; i--)
//        {
//             rev = rev +  str.charAt(i);
//        }
//        System.out.println(rev);
    }
}
